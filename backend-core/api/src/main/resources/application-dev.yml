spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartmatch_db_dev?createDatabaseIfNotExist=true
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.smartmatch: DEBUG